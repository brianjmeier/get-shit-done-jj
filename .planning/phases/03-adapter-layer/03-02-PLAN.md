---
phase: 03-adapter-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [adapters/opencode/command/gsd/*.md]
autonomous: true
---

<objective>
Create OpenCode adapter by porting high-priority commands with adjusted paths.

Purpose: Enable GSD-JJ to work with OpenCode by creating client-specific command files that reference the shared core.
Output: OpenCode command files in adapters/opencode/command/gsd/ directory.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@commands/gsd/execute-plan.md
@commands/gsd/new-project.md
@commands/gsd/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenCode adapter directory structure</name>
  <files>adapters/opencode/command/gsd/.gitkeep, adapters/opencode/README.md</files>
  <action>
    Create the OpenCode adapter directory structure:

    ```
    adapters/
      opencode/
        README.md           # OpenCode-specific installation and usage
        command/
          gsd/              # OpenCode slash commands
            .gitkeep
    ```

    Create adapters/opencode/README.md with:
    - OpenCode installation instructions
    - Path: commands install to `~/.config/opencode/command/gsd/`
    - Shared core installs to `~/.config/opencode/get-shit-done/`
    - Reference to main README for general GSD usage

    Use mkdir -p for directory creation.
  </action>
  <verify>ls -la adapters/opencode/command/gsd/ shows directory exists</verify>
  <done>OpenCode adapter directory structure created</done>
</task>

<task type="auto">
  <name>Task 2: Port core commands to OpenCode format</name>
  <files>adapters/opencode/command/gsd/execute-plan.md, adapters/opencode/command/gsd/new-project.md, adapters/opencode/command/gsd/progress.md</files>
  <action>
    Port these high-priority commands to OpenCode format:
    - execute-plan.md
    - new-project.md
    - progress.md

    For each command:
    1. Copy from commands/gsd/{command}.md
    2. Replace all `~/.claude/get-shit-done/` with `~/.config/opencode/get-shit-done/`
    3. Update frontmatter if needed for OpenCode compatibility
    4. Keep all other content identical

    The pattern is simple: change the path prefix for @references.

    Example transformation:
    - FROM: `@~/.claude/get-shit-done/workflows/execute-plan.md`
    - TO:   `@~/.config/opencode/get-shit-done/workflows/execute-plan.md`
  </action>
  <verify>grep -c "config/opencode" adapters/opencode/command/gsd/execute-plan.md shows path updates</verify>
  <done>Core commands ported with updated paths</done>
</task>

<task type="auto">
  <name>Task 3: Port remaining essential commands</name>
  <files>adapters/opencode/command/gsd/plan-phase.md, adapters/opencode/command/gsd/execute-phase.md, adapters/opencode/command/gsd/help.md</files>
  <action>
    Port remaining essential commands:
    - plan-phase.md
    - execute-phase.md
    - help.md

    Same transformation as Task 2:
    1. Copy from commands/gsd/
    2. Replace `~/.claude/` with `~/.config/opencode/`
    3. Verify all @references updated

    These 6 commands (3 from Task 2 + 3 from Task 3) cover the core GSD workflow:
    new-project → plan-phase → execute-plan/execute-phase → progress
  </action>
  <verify>ls adapters/opencode/command/gsd/*.md | wc -l shows 6 commands</verify>
  <done>All essential commands ported to OpenCode format</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] adapters/opencode/command/gsd/ directory exists
- [ ] 6 command files created
- [ ] All paths updated from ~/.claude/ to ~/.config/opencode/
- [ ] README.md created in adapters/opencode/
- [ ] All changes committed atomically
</verification>

<success_criteria>

- OpenCode adapter directory structure in place
- 6 essential commands ported
- All path references correctly updated
- OpenCode-specific README created
</success_criteria>

<output>
After completion, create `.planning/phases/03-adapter-layer/03-02-SUMMARY.md`
</output>
