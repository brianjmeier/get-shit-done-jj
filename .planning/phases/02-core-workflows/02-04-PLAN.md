---
phase: 02-core-workflows
plan: 04
type: execute
wave: 2
depends_on: ["02-02"]
files_modified: [commands/gsd/pause-work.md, commands/gsd/check-todos.md, commands/gsd/add-todo.md, commands/gsd/remove-phase.md, commands/gsd/complete-milestone.md]
autonomous: true
---

<objective>
Update remaining commands with git references to use JJ commands.

Purpose: These supporting commands have 1-5 git references each. Converting them completes the command layer.

Output: All remaining commands using JJ patterns
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-jj-foundation/01-01-SUMMARY.md

# JJ reference
@get-shit-done/references/jj-integration.md

# Files to modify
@commands/gsd/pause-work.md
@commands/gsd/check-todos.md
@commands/gsd/add-todo.md
@commands/gsd/remove-phase.md
@commands/gsd/complete-milestone.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pause-work.md command for JJ</name>
  <files>commands/gsd/pause-work.md</files>
  <action>
Replace git commands (2 references):

- `git status` → `jj status`
- `git add/commit` for handoff → `jj commit -m "wip: ..."`

Use handoff commit format from jj-integration.md.
  </action>
  <verify>
grep -c "git " commands/gsd/pause-work.md should return 0
  </verify>
  <done>
pause-work.md uses JJ for handoff commits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update check-todos.md and add-todo.md commands for JJ</name>
  <files>commands/gsd/check-todos.md, commands/gsd/add-todo.md</files>
  <action>
Replace git commands in both files:

**check-todos.md (5 references):**
- `git status` → `jj status`
- `git diff` → `jj diff`
- Any commit commands → `jj commit`

**add-todo.md (4 references):**
- Similar pattern replacements
- Remove staging commands
  </action>
  <verify>
grep -c "git " commands/gsd/check-todos.md should return 0
grep -c "git " commands/gsd/add-todo.md should return 0
  </verify>
  <done>
Both todo commands use JJ for status checks and commits.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update remove-phase.md and complete-milestone.md commands</name>
  <files>commands/gsd/remove-phase.md, commands/gsd/complete-milestone.md</files>
  <action>
Replace git commands in both files:

**remove-phase.md (5 references):**
- `git status` → `jj status`
- `git add/commit` → `jj commit`

**complete-milestone.md command (4 references):**
- Update to match workflow changes from Plan 03
- `git tag` → JJ bookmark pattern
- `git push` → `jj git push`
  </action>
  <verify>
grep -c "git " commands/gsd/remove-phase.md should return 0
grep -c "git " commands/gsd/complete-milestone.md should return 0
  </verify>
  <done>
remove-phase.md and complete-milestone.md commands use JJ.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] pause-work.md has no git references
- [ ] check-todos.md has no git references
- [ ] add-todo.md has no git references
- [ ] remove-phase.md has no git references
- [ ] complete-milestone.md command has no git references
- [ ] All commands use JJ patterns from jj-integration.md
</verification>

<success_criteria>
- All 5 command files converted to JJ
- No git references remain in any command file
- Commit message formats preserved
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-workflows/02-04-SUMMARY.md`
</output>
